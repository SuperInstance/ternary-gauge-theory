[package]
name = "ternary-gauge-theory"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Z₃ lattice gauge theory with ternary conservation law verification"

[dependencies]
rand = "0.8"

[dev-dependencies]
rand_chacha = "0.3"
